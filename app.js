const pi=Math.PI;
let r=15;

//let area=pi*r*r;
//let circ=pi*2*r;
//console.log(area.toFixed(2), circ.toFixed(2));
//console.log(`Area is ${area.toFixed(2)} and circ is ${circ.toFixed(2)}`)

console.log(` r is ${r}, Area is ${ (pi*r*r).toFixed(2) } and circ is ${ (pi * 2 * r).toFixed(2)}`)